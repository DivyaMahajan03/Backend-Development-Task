1) Tech Stack
  - FastAPI
  - MySQL
  - SQLAlchemy
  - JWT Authentication

2) Setup
  git clone repo
  pip install -r requirements.txt
  uvicorn app.main:app --reload

3) Database Setup
   - Run schema.sql
